layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="zellij:tab-bar"
        }
        children
        pane size=2 borderless=true {
            plugin location="zellij:status-bar"
        }
    }

    tab name="Neovim" focus=true {
        pane command="bash" {
            args "-lc" "exec \"$DEV_WORKBENCH_DIR/scripts/dev-tab.sh\" editor"
        }
    }
    tab name="Codex" {
        pane command="bash" {
            args "-lc" "exec \"$DEV_WORKBENCH_DIR/scripts/dev-tab.sh\" codex"
        }
    }
    tab name="Git" {
        pane command="bash" {
            args "-lc" "exec \"$DEV_WORKBENCH_DIR/scripts/dev-tab.sh\" git"
        }
    }
    tab name="Test" {
        pane command="bash" {
            args "-lc" "exec \"$DEV_WORKBENCH_DIR/scripts/dev-tab.sh\" test"
        }
    }
}
